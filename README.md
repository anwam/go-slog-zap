# Golang log/slog and zap integration

I'm try to integrate log/slog and zap logger. To use zap logger as a backend for slog. And use slog as a wrapper for zap.

## Usage

```shell
go run .
```

## Packages

- log/slog
- github.com/uber-go/zap
